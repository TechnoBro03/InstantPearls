# These functions run AT the end of the ray (where it hits the max steps) AS the player
# Ray is already stopped

#particle explosion ~ ~ ~ 0.5 0.5 0.5 0 3 force

function better_pearls:ray_cast/hit/hit_block